<form method="post" action="{{ path('app_profil_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" class="mt-5 pt-5">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-warning">Supprimer définitivement mon compte </button>
</form>
